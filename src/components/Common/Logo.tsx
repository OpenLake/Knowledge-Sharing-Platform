import { FC } from 'react'

export const Logo: FC<{}> = ({}) => {
    return (
        <div>
            <span className="font-extrabold bg-gradient-to-r from-primary to-primary-light bg-clip-text text-transparent text-4xl">
                KSP
            </span>
        </div>
    )
}
