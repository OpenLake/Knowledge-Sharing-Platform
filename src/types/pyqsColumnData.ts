export interface pyqsColumnData {
    sno: string
    upvotes: number
    title: string
    subjectCode: string
    subjectName: string
    url: string
    semester: string
    instructor: string
    branch: string
    uploadedBy: string
    actions: JSX.Element
}
