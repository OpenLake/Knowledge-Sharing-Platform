export interface notesColumnData {
    sno: string
    title: string
    subjectCode: string
    subjectName: string
    url: string
    class: string
    batch: string
    branch: string
    uploadedBy: string
    actions: JSX.Element
}
